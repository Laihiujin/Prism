# hermes-agent 组件环境（隔离，固定给 hermes 用）。
# 组件本体（tools/hermes-agent）由 provision.py 用
#   pip install -e tools/hermes-agent
# 装入本环境并生成 bin/hermes 入口——其依赖（fastapi/uvicorn/openai/websockets 等）
# 一并装进来，只会落在本环境里，不污染共享 prismenv。
name: prism-hermes
channels:
{{CHANNELS}}
dependencies:
  - python=3.11
  - pip
