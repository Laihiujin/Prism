# ============================================
# Prism - 精简运行时依赖
# 仅保留被源码直接 import 的包 + 隐式运行时依赖；
# 其余为传递依赖，由 pip 自动解析，无需在此列出。
# 开发/测试工具见 requirements-dev.txt
# ============================================

# ---- Web 框架（版本与已验证的 .venv_test 对齐，避免新版 fastapi 路由注册不兼容）----
fastapi==0.133.1
uvicorn[standard]==0.41.0
starlette==1.3.1
python-multipart          # fastapi File/UploadFile/Form 解析所需（隐式运行时依赖）

# ---- 数据与配置 ----
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv
pyyaml
toml

# ---- 数据库 ----
sqlalchemy==2.0.52

# ---- 缓存与任务队列 ----
redis
celery<5.5.0

# ---- 浏览器自动化 ----
patchright==1.59.1
selenium

# ---- HTTP 客户端 ----
httpx
requests
aiohttp
aiofiles

# ---- AI 服务 ----
openai
tenacity

# ---- 平台 SDK ----
biliup
xhs

# ---- 抖音/快手接口集成 ----
browser-cookie3
gmssl
importlib_resources
pyfiglet
pywebio
pywebio-battery
websockets

# ---- 媒体与图像 ----
numpy
opencv-python
qrcode[pil]
segno>=1.6.6

# ---- 办公/文档 ----
openpyxl

# ---- 工具 ----
loguru
psutil
pytz
rich
schedule
imageio-ffmpeg==0.6.0      # 自带跨平台静态 ffmpeg 二进制,供封面截取/视频转码(find_ffmpeg)使用
